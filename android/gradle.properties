org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# Mapbox Maps SDK: set SDK_REGISTRY_TOKEN in ~/.gradle/gradle.properties (NOT here)
# with your secret download token (sk.ey...) from https://account.mapbox.com/access-tokens/
# The token requires the DOWNLOADS:READ scope.
# See: https://docs.mapbox.com/android/maps/guides/install/
